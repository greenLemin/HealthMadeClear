# src/app/api/og/

## Responsibility

Open Graph image generation endpoint for social share previews.

## Integration

- Consumed by: Page metadata (og:image tags)
- Depends on: `@vercel/og` or similar image generation library
