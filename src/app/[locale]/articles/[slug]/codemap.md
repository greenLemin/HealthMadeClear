# src/app/[locale]/articles/[slug]/

## Responsibility

Article detail page — renders full article content with sections and callouts.

## Integration

- Uses `getArticleByIdFromBundle` from `src/lib/articles/loadArticles.ts`
- Renders via MDX components
