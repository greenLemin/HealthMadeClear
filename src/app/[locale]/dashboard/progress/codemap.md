# src/app/[locale]/dashboard/progress/

## Responsibility

Detailed progress page with paginated lesson completion history and quiz scores.

## Integration

- Uses `getCompletedLessonsPaginated` from `src/lib/dashboard.ts`
- Server component with URL-based pagination
