# Canonical site URL for sitemap and Open Graph metadata (required in production CI)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Optional: Sentry DSN for client error reporting (no PHI is sent)
# NEXT_PUBLIC_SENTRY_DSN=
